{% extends 'base.html.twig' %}

{% block title %}Edition Session{% endblock %}

{% block body %}
    <h1>Editer Session</h1>

    {{ include('session/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('session_index') }}">Retour à la liste</a>

    {#{% if isCurrentPath('session_edit') %}
        {{ include('session/_delete_form.html.twig') }}
    {% endif %}#}
  
    {% if mode == "edit" %}
        {# include('session/_delete_form.html.twig') #}
        <a href="{{ path('session_delete', { 'id' : session.id }) }}" onclick="return confirm('Sur de vouloir supprimer {{ session.title }}?');">Supprimer cette session</a>
    {% endif %}
    
{% endblock %}
{% block javascripts %}
{{ parent ()}}
<script src="{{asset('js/CollectionProg.js')}}"></script>
{% endblock %}